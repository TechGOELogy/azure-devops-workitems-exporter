<html>
  <head>
    <title> Work Item Export </title>
  </head>
  <body>
    {{ for work_item in work_items }}
      <h2> {{ work_item.fields["System.WorkItemType"] }} - {{ work_item.fields["System.Id"] }} : {{ work_item.fields["System.Title"] }} </h2>
      <b> State </b> : {{ work_item.fields["System.State"] ?? "Undefined" }} <br>
      <b> Assigned To </b> : &nbsp;
      {{ assigned = work_item.fields["System.AssignedTo"] }}
      {{ display_name = "" }}

      {{ for item in assigned }}
        {{ if item.key == "displayName" }}
          {{ display_name = item.value }}
        {{ end }}
      {{ end }}

      {{ display_name }}

      <br>

      {{ if work_item.children.size > 0 }}
        {{ for child in work_item.children }}
          <h3> {{ child.fields["System.WorkItemType"] }} - {{ child.fields["System.Id"] }} : {{ child.fields["System.Title"] }} </h3>
          <b> State </b> : {{ child.fields["System.State"] ?? "Undefined" }} <br>
          <b> Assigned To </b> : &nbsp;
          {{ assigned = work_item.fields["System.AssignedTo"] }}
          {{ display_name = "" }}

          {{ for item in assigned }}
            {{ if item.key == "displayName" }}
              {{ display_name = item.value }}
            {{ end }}
          {{ end }}

          {{ display_name }}
        {{ end }}
      {{ end }}

    {{ end }}
  </body>
</html>